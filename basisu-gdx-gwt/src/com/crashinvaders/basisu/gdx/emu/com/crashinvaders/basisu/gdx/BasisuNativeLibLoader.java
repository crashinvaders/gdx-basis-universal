package com.crashinvaders.basisu.gdx;

public class BasisuNativeLibLoader {
    public static synchronized void loadIfNeeded() {
        // GWT native libs are declared and loaded through an HTML header <script> element.
        // We don't need to load anything at this point.
    }
}
